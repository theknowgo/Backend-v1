import priceController from "../controllers/price.Controller.js";
import express from "express";
const router = express.Router();

// router.get(
//   "/service-and-product-price",
//   priceController.serviceAndProductPrice
// );
// router.get("/capped-time-for-info", priceController.cappedTimeForInfo);
// router.get(
//   "/capped-time-for-info-incremental",
//   priceController.incrementalTimeForInfo
// );
// router.get("/quick-lockup-cost", priceController.quickLookup);

export default router;
